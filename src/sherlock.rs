use color_eyre::Result;

pub fn sherlock(username: String, site_data: Vec<String>) -> Result<()> {
    // start with happy-path without tor or anything

    // for social_network, net_info in site_data.items():
    //   add correct headers
    //   interpolate string with the username
    //   use regex to make sure the url and username are valid for the site
    //   use urlProbe as well to check
    //   make the request (with or without proxy)

    // for social_network, net_info in site_data.items():
    //    await response and add site data to output data struct
    //    record metrics like response time, status, errors, etc

    Ok(())
}
